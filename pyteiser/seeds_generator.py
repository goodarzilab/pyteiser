import numpy as np

import structures

def get_next_motif(motif, last_letter = 'N'):
    for i in range(motif.linear_length):
        if motif[i].nucl < last_letter:
            numpy uint8 next one


aa = structures.s_motif()


